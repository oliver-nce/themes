{
    "version": 1,
    "description": "Machine-readable index of every module in the Themes Frappe app. Read this file FIRST before exploring the codebase — it tells you exactly where to look for any given concern.",
    "modules": {
        "themes": {
            "purpose": "Frappe custom app for site-wide CSS theming",
            "entry_points": {
                "hooks.py": "Frappe app metadata, CSS loading, SPA routing, Apps screen entry",
                "api.py": "Whitelisted API: regenerate_theme_css()",
                "init_theme.py": "One-time theme initialisation script: creates Theme Settings with defaults and regenerates CSS. Run via bench execute."
            },
            "utils": {
                "utils/theme_color_utils.py": "Color math helpers: OKLCH shade generation, shadow building, token maps (BORDER_RADIUS_MAP, etc.)",
                "utils/css_writer.py": "CSS generation and publish: generate_css(), publish_version() writes nce_theme.css"
            },
            "doctypes": {
                "themes/doctype/theme_settings/": "Legacy Single doctype holding theme tokens. on_update() is a no-op (deprecated); CSS publishing moves to Theme Version + Site Theme Config."
            },
            "static_files": {
                "public/css/": "Generated theme CSS (nce_theme.css)",
                "public/frontend/": "Vue SPA build output (regenerated by Vite)",
                "www/themes.html": "SPA entry point template"
            }
        },
        "frontend": {
            "purpose": "Vue 3 SPA for theme configuration and preview",
            "key_files": {
                "src/router.ts": "Vue Router config — maps /themes/* paths to page components",
                "src/main.ts": "App entry point",
                "src/App.vue": "Root component with sidebar navigation"
            },
            "pages": {
                "src/pages/ThemeSettingsPage.vue": "Main theme configuration UI (tabbed: Colors, Typography, Layout, Advanced)",
                "src/pages/ThemePreviewPage.vue": "Live theme preview page",
                "src/pages/HomePage.vue": "Landing page with links to settings and preview"
            },
            "composables": {
                "src/composables/useTheme.ts": "Loads Theme Settings via frappe-ui createResource and applies CSS vars to document root",
                "src/composables/useThemeDefaults.ts": "Default theme color/typography values"
            },
            "components": {
                "src/components/BrandColorPicker.vue": "Brand color picker with swatch presets",
                "src/components/SwatchPicker.vue": "Color swatch selection component",
                "src/components/ThemePreview.vue": "Mini theme preview component"
            },
            "utilities": {
                "src/utils/theme-injector.ts": "CSS variable injection utility",
                "src/utils/color-shades.ts": "OKLCH shade generation (11-stop scale)",
                "tailwind.config.js": "Maps Tailwind utilities to --nce-* CSS tokens"
            }
        }
    },
    "architecture_notes": [
        "Theme uses CSS custom properties (--nce-*) injected as :root vars — downstream apps consume via var()",
        "Theme CSS is loaded site-wide via app_include_css in hooks.py",
        "Vue SPA is served at /themes/* routes via www/themes.html"
    ]
}
